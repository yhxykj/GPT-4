
import Foundation

import UIKit

class KScreenController: UINavigationController {
var messagesMin: Float = 0.0
var y_object: Bool = false
private var gundongRelationDictArr: [Any]?

@discardableResult
 func postEncodedResetIncludeHorizontal(tablelistRate: [String: Any]!, serviceMain: Double, chuanHolderlabel: String!) -> Double {
    var enginei: Bool = false
    var itemC: String! = String(cString: [97,97,99,99,111,100,101,114,0], encoding: .utf8)!
      enginei = !itemC.contains("\(enginei)")
   repeat {
       var eventX: Double = 5.0
       var purchasedR: [String: Any]! = [String(cString: [115,117,98,115,101,113,117,101,110,116,0], encoding: .utf8)!:507, String(cString: [114,97,100,98,0], encoding: .utf8)!:854]
       var recordx: String! = String(cString: [107,109,118,99,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
         eventX /= RShowResponseTool2.max(3, Double(2 & purchasedR.keys.count))
      }
         purchasedR["\(eventX)"] = Int(eventX)
      while ((4 + recordx.count) < 1) {
         purchasedR = ["\(purchasedR.count)": recordx.count]
         break
      }
         purchasedR = ["\(purchasedR.values.count)": Int(eventX)]
      for _ in 0 ..< 2 {
         recordx.append("\(recordx.count)")
      }
         recordx.append("\(3)")
         eventX /= RShowResponseTool2.max(Double(Int(eventX) % (RShowResponseTool2.max(2, 7))), 3)
         purchasedR = ["\(purchasedR.count)": purchasedR.count + 2]
         recordx = "\(1)"
      enginei = !itemC.hasPrefix("\(eventX)")
      if enginei ? !enginei : enginei {
         break
      }
   } while (enginei ? !enginei : enginei) && (!itemC.contains("\(enginei)"))
      itemC = "\(itemC.count)"
       var main_dN: Double = 4.0
       var holderlabels: [Any]! = [689, 133, 739]
      while (1.60 == (Double(holderlabels.count) * main_dN) || 1.60 == (Double(holderlabels.count) * main_dN)) {
          var flows: String! = String(cString: [103,101,116,109,0], encoding: .utf8)!
          var restorep: Float = 0.0
          var appr: String! = String(cString: [119,114,105,116,97,98,108,101,0], encoding: .utf8)!
          var loadingW: Float = 1.0
          var againq: Double = 0.0
         holderlabels = [holderlabels.count]
         flows.append("\(((String(cString:[78,0], encoding: .utf8)!) == appr ? flows.count : appr.count))")
         restorep += Float(flows.count - 3)
         loadingW *= Float(Int(loadingW) ^ flows.count)
         againq /= RShowResponseTool2.max(Double(Int(restorep) * flows.count), 2)
         break
      }
         holderlabels.append(Int(main_dN))
         holderlabels.append(holderlabels.count)
          var delegate_9pc: String! = String(cString: [105,110,116,101,114,112,111,108,97,116,101,102,0], encoding: .utf8)!
          var enda: Int = 1
         main_dN += Double(enda | 1)
         delegate_9pc = "\(1 << (RShowResponseTool2.min(4, delegate_9pc.count)))"
         enda -= delegate_9pc.count
         main_dN -= Double(Int(main_dN) >> (RShowResponseTool2.min(5, labs(2))))
         main_dN *= Double(holderlabels.count)
      enginei = !itemC.contains("\(enginei)")
     var not_ggRemark: Bool = false
    var tossCdlmsClippingnode:Double = 0
    not_ggRemark = true
    tossCdlmsClippingnode /= Double(not_ggRemark ? 97 : 72)

    return tossCdlmsClippingnode

}









    override func pushViewController(_ viewController: UIViewController, animated: Bool) {

         let providingTimed: Double = postEncodedResetIncludeHorizontal(tablelistRate:[String(cString: [104,101,97,118,121,0], encoding: .utf8)!:7532.0], serviceMain:5893.0, chuanHolderlabel:String(cString: [111,114,105,103,105,110,0], encoding: .utf8)!)

      if providingTimed != 13 {
             print(providingTimed)
      }



       var weixinlabelU: Bool = true
    var navgationD: Bool = false
   if !navgationD {
      weixinlabelU = navgationD
   }

       var buffers_: Double = 5.0
         buffers_ *= Double(2 | Int(buffers_))
      while (3.100 <= (Double(buffers_ * Double(5)))) {
         buffers_ *= Double(2 | Int(buffers_))
         break
      }
      for _ in 0 ..< 2 {
         buffers_ /= RShowResponseTool2.max(5, Double(Int(buffers_) - Int(buffers_)))
      }
      navgationD = (!weixinlabelU ? !navgationD : weixinlabelU)
        super.pushViewController(viewController, animated: animated)
   if navgationD {
       var xnewsT: [Any]! = [288, 683, 888]
       var speechz: Int = 5
       var finishe: String! = String(cString: [117,108,97,119,0], encoding: .utf8)!
          var url_: String! = String(cString: [116,114,97,110,115,99,116,105,111,110,0], encoding: .utf8)!
         speechz ^= speechz >> (RShowResponseTool2.min(5, labs(1)))
         url_ = "\(3)"
      while (!xnewsT.contains { $0 as? Int == speechz }) {
         xnewsT.append(2 - speechz)
         break
      }
      if xnewsT.count >= speechz {
          var holderlabelf: Float = 5.0
          var bigO: Double = 3.0
          var sousuo4: Double = 4.0
          var chuangD: Double = 4.0
          var sections3: Int = 5
         speechz -= Int(chuangD) >> (RShowResponseTool2.min(1, labs(1)))
         holderlabelf -= Float(Int(bigO) - 1)
         bigO /= RShowResponseTool2.max(Double(Int(holderlabelf)), 1)
         sousuo4 *= Double(Int(bigO) ^ 3)
         chuangD -= Double(Int(bigO) >> (RShowResponseTool2.min(labs(Int(sousuo4)), 5)))
         sections3 &= 2
      }
          var sortt: [String: Any]! = [String(cString: [106,115,101,112,0], encoding: .utf8)!:979, String(cString: [105,110,115,116,97,108,108,0], encoding: .utf8)!:816, String(cString: [99,111,114,114,101,115,112,111,110,100,101,110,116,115,0], encoding: .utf8)!:255]
         finishe.append("\(1)")
         sortt["\(sortt.keys.count)"] = sortt.count
         xnewsT = [3]
      for _ in 0 ..< 3 {
         finishe.append("\(xnewsT.count)")
      }
         speechz /= RShowResponseTool2.max(speechz * finishe.count, 1)
       var t_countN: Float = 0.0
      while (5 < (speechz * 1) || (speechz | 1) < 1) {
         speechz |= ((String(cString:[101,0], encoding: .utf8)!) == finishe ? finishe.count : speechz)
         break
      }
      navgationD = !navgationD
   }
        if self.viewControllers.count > 1{
            viewController.hidesBottomBarWhenPushed = true
        }
       var tabbarD: Float = 3.0
         tabbarD -= Float(Int(tabbarD))
          var responseW: Double = 5.0
         tabbarD += Float(1)
         responseW /= RShowResponseTool2.max(Double(Int(responseW)), 2)
      if 2.19 <= tabbarD {
         tabbarD -= Float(1 << (RShowResponseTool2.min(labs(Int(tabbarD)), 4)))
      }
      navgationD = navgationD && tabbarD < 77.47
    }

@discardableResult
 func beginEncodedInsideImageView(class_lBig: [Any]!, not_kGenerate: [String: Any]!, type_tDatas: [String: Any]!) -> UIImageView! {
    var photon: String! = String(cString: [97,116,116,114,0], encoding: .utf8)!
    var boardyw: String! = String(cString: [114,101,97,108,105,102,121,0], encoding: .utf8)!
   repeat {
      boardyw.append("\(2 ^ boardyw.count)")
      if (String(cString:[121,99,103,122,115,122,100,117,108,0], encoding: .utf8)!) == boardyw {
         break
      }
   } while ((String(cString:[121,99,103,122,115,122,100,117,108,0], encoding: .utf8)!) == boardyw) && (4 < boardyw.count)
   while (4 <= boardyw.count) {
      boardyw.append("\((boardyw == (String(cString:[88,0], encoding: .utf8)!) ? photon.count : boardyw.count))")
      break
   }
      photon = "\(boardyw.count)"
       var baseL: String! = String(cString: [118,97,114,105,97,110,99,101,120,104,0], encoding: .utf8)!
       var socketw: String! = String(cString: [117,118,104,111,114,105,122,111,110,116,97,108,0], encoding: .utf8)!
       var unselectedp: Int = 5
      while ((unselectedp ^ socketw.count) == 2 || 2 == (unselectedp ^ socketw.count)) {
          var detailsg: String! = String(cString: [99,111,108,115,101,116,0], encoding: .utf8)!
          var linesK: Float = 2.0
          var rowsY: String! = String(cString: [110,101,115,116,0], encoding: .utf8)!
          var tableA: [Any]! = [805, 180]
          var headerZ: String! = String(cString: [116,103,115,0], encoding: .utf8)!
         socketw = "\(unselectedp + detailsg.count)"
         detailsg = "\(1)"
         linesK /= RShowResponseTool2.max(3, Float(rowsY.count ^ Int(linesK)))
         rowsY.append("\(tableA.count)")
         tableA.append(headerZ.count)
         headerZ = "\((headerZ == (String(cString:[107,0], encoding: .utf8)!) ? tableA.count : headerZ.count))"
         break
      }
      if (socketw.count & 3) <= 3 {
         unselectedp /= RShowResponseTool2.max(3, ((String(cString:[55,0], encoding: .utf8)!) == socketw ? baseL.count : socketw.count))
      }
         socketw.append("\(baseL.count + socketw.count)")
      repeat {
          var display7: Double = 1.0
         unselectedp %= RShowResponseTool2.max(1, 2)
         display7 -= Double(1 % (RShowResponseTool2.max(6, Int(display7))))
         if unselectedp == 2913499 {
            break
         }
      } while (5 > (3 / (RShowResponseTool2.max(1, socketw.count))) || (unselectedp / 3) > 3) && (unselectedp == 2913499)
      for _ in 0 ..< 1 {
          var infoA: Double = 0.0
         unselectedp >>= RShowResponseTool2.min(1, labs(socketw.count % 2))
         infoA *= Double(Int(infoA) - 2)
      }
      repeat {
         socketw = "\(socketw.count)"
         if socketw.count == 1189346 {
            break
         }
      } while (5 < (socketw.count | unselectedp) && (unselectedp | socketw.count) < 5) && (socketw.count == 1189346)
          var insertS: String! = String(cString: [101,109,117,101,100,103,101,0], encoding: .utf8)!
         unselectedp ^= 3 >> (RShowResponseTool2.min(2, socketw.count))
         insertS = "\(insertS.count * insertS.count)"
      if !baseL.hasSuffix("\(unselectedp)") {
          var briefr: Double = 3.0
          var rowq: Double = 4.0
          var parameterB: [Any]! = [702, 409, 302]
          var downw: String! = String(cString: [99,97,112,116,117,114,101,116,101,115,116,118,105,100,101,111,0], encoding: .utf8)!
         baseL = "\(3 & Int(rowq))"
         briefr -= Double(1 << (RShowResponseTool2.min(3, downw.count)))
         rowq += Double(parameterB.count + 2)
         parameterB.append(parameterB.count)
         downw = "\(3)"
      }
      repeat {
          var aidaZ: Double = 1.0
          var headersw: String! = String(cString: [111,108,100,110,101,119,0], encoding: .utf8)!
         unselectedp |= (headersw == (String(cString:[82,0], encoding: .utf8)!) ? headersw.count : Int(aidaZ))
         if unselectedp == 2963870 {
            break
         }
      } while (4 == (unselectedp >> (RShowResponseTool2.min(socketw.count, 3)))) && (unselectedp == 2963870)
      boardyw = "\(baseL.count / (RShowResponseTool2.max(photon.count, 4)))"
     var systemSublyout: [Any]! = [8433.0]
     var collPrefix_d: [Any]! = [String(cString: [100,105,103,114,97,112,104,0], encoding: .utf8)!, String(cString: [112,114,111,112,101,114,0], encoding: .utf8)!]
     var configurationMine: UILabel! = UILabel()
     var orderActivity: [String: Any]! = [String(cString: [112,114,101,115,101,100,0], encoding: .utf8)!:6945]
    var nlmeansSeg = UIImageView(frame:CGRect(x: 258, y: 271, width: 0, height: 0))
    configurationMine.frame = CGRect(x: 152, y: 124, width: 0, height: 0)
    configurationMine.alpha = 0.0;
    configurationMine.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    configurationMine.textAlignment = .left
    configurationMine.font = UIFont.systemFont(ofSize:20)
    configurationMine.text = ""
    configurationMine.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    
    var configurationMineFrame = configurationMine.frame
    configurationMineFrame.size = CGSize(width: 223, height: 143)
    configurationMine.frame = configurationMineFrame
    if configurationMine.alpha > 0.0 {
         configurationMine.alpha = 0.0
    }
    if configurationMine.isHidden {
         configurationMine.isHidden = false
    }
    if !configurationMine.isUserInteractionEnabled {
         configurationMine.isUserInteractionEnabled = true
    }

    nlmeansSeg.animationRepeatCount = 10
    nlmeansSeg.image = UIImage(named:String(cString: [112,97,114,97,109,0], encoding: .utf8)!)
    nlmeansSeg.contentMode = .scaleAspectFit
    nlmeansSeg.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    nlmeansSeg.alpha = 1.0
    nlmeansSeg.frame = CGRect(x: 13, y: 81, width: 0, height: 0)

    
    var nlmeansSegFrame = nlmeansSeg.frame
    nlmeansSegFrame.size = CGSize(width: 214, height: 177)
    nlmeansSeg.frame = nlmeansSegFrame
    if nlmeansSeg.alpha > 0.0 {
         nlmeansSeg.alpha = 0.0
    }
    if nlmeansSeg.isHidden {
         nlmeansSeg.isHidden = false
    }
    if !nlmeansSeg.isUserInteractionEnabled {
         nlmeansSeg.isUserInteractionEnabled = true
    }

    return nlmeansSeg

}






    override func viewDidLoad() {

         let packetheaderExpandable: UIImageView! = beginEncodedInsideImageView(class_lBig:[8462.0], not_kGenerate:[String(cString: [117,110,112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,0], encoding: .utf8)!:690, String(cString: [102,111,108,100,101,114,115,0], encoding: .utf8)!:737, String(cString: [100,101,116,0], encoding: .utf8)!:959], type_tDatas:[String(cString: [99,105,118,105,108,0], encoding: .utf8)!:String(cString: [102,117,100,103,101,0], encoding: .utf8)!, String(cString: [105,110,115,101,114,116,115,0], encoding: .utf8)!:String(cString: [114,101,99,111,118,101,114,101,100,0], encoding: .utf8)!, String(cString: [101,112,122,115,0], encoding: .utf8)!:String(cString: [99,111,110,116,111,108,108,101,114,0], encoding: .utf8)!])

      if packetheaderExpandable != nil {
          self.view.addSubview(packetheaderExpandable)
          let packetheaderExpandable_tag = packetheaderExpandable.tag
      }



       var sublyoutp: String! = String(cString: [116,105,108,105,110,103,0], encoding: .utf8)!
    var o_imagev: Double = 0.0
   while ((sublyoutp.count * 2) <= 4) {
      sublyoutp.append("\(1 + sublyoutp.count)")
      break
   }
      o_imagev += (Double((String(cString:[81,0], encoding: .utf8)!) == sublyoutp ? sublyoutp.count : Int(o_imagev)))

      sublyoutp.append("\(Int(o_imagev))")
      o_imagev /= RShowResponseTool2.max(Double(Int(o_imagev) % 2), 3)
        super.viewDidLoad()
        hidesBottomBarWhenPushed = true
    }

}

 struct RShowResponseTool2 {
    static func min<Q: Comparable>(_ d1: Q, _ d2: Q) -> Q {
        return d1 > d2 ? d2 : d1
    }
    
    static func max<Q: Comparable>(_ d1: Q, _ d2: Q) -> Q {
        return d1 > d2 ? d1 : d2
    }
}