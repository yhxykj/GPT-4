
import Foundation

import UIKit

class WUTImageController: UITabBarController {
var brief_index: Int = 0
private var doneOpenFinished_dictionary: [String: Any]!

@discardableResult
 func appQuestionFormat(primeContents: Float, speechHome: [String: Any]!, currentTerminate: Bool) -> Double {
    var while_ioi: Bool = false
    var connectV: String! = String(cString: [114,101,115,112,111,110,100,115,0], encoding: .utf8)!
   if connectV.count == 1 || !while_ioi {
      while_ioi = connectV.count == 6 || while_ioi
   }
       var safeZ: String! = String(cString: [110,98,105,111,0], encoding: .utf8)!
       var data4: String! = String(cString: [108,101,114,112,105,110,103,0], encoding: .utf8)!
       var againd: Double = 4.0
         safeZ = "\(3 | safeZ.count)"
      for _ in 0 ..< 3 {
         againd -= Double(1 * data4.count)
      }
         data4.append("\(data4.count - 3)")
         safeZ.append("\(1 * Int(againd))")
         safeZ.append("\(Int(againd) % (KCenterTool17.max(data4.count, 8)))")
       var x_countI: Int = 5
      if safeZ.count > data4.count {
         safeZ = "\(x_countI & Int(againd))"
      }
      while (3 >= (4 & data4.count) && (Int(againd) / 4) >= 3) {
          var free8: Float = 4.0
          var qinit_8s1: [String: Any]! = [String(cString: [104,112,114,101,100,0], encoding: .utf8)!:913, String(cString: [120,102,101,114,0], encoding: .utf8)!:898, String(cString: [116,105,109,101,102,105,108,116,101,114,95,57,95,50,57,0], encoding: .utf8)!:336]
         data4.append("\(Int(againd) ^ safeZ.count)")
         free8 /= KCenterTool17.max(2, Float(qinit_8s1.keys.count))
         qinit_8s1["\(free8)"] = qinit_8s1.values.count
         break
      }
          var window_ck: String! = String(cString: [119,97,108,115,104,120,0], encoding: .utf8)!
         data4 = "\(Int(againd))"
         window_ck.append("\(3)")
      while_ioi = data4.count <= 13 && !while_ioi
   if 3 > connectV.count || !while_ioi {
      while_ioi = !while_ioi
   }
     let successRestore: Double = 781.0
    var iterateEnforcePersisted:Double = 0
    iterateEnforcePersisted /= Swift.max(successRestore, 1)

    return iterateEnforcePersisted

}









    override func viewDidLoad() {

         let libavdevicePerspective: Double = appQuestionFormat(primeContents:3981.0, speechHome:[String(cString: [102,105,108,108,112,0], encoding: .utf8)!:726, String(cString: [115,116,109,116,0], encoding: .utf8)!:905], currentTerminate:true)

      if libavdevicePerspective > 55 {
             print(libavdevicePerspective)
      }



       var navigation9: String! = String(cString: [109,112,101,103,118,105,100,101,111,101,110,99,100,115,112,0], encoding: .utf8)!
    var z_animation0: String! = String(cString: [115,111,114,116,101,114,0], encoding: .utf8)!
      z_animation0.append("\(((String(cString:[79,0], encoding: .utf8)!) == z_animation0 ? z_animation0.count : navigation9.count))")

       var safel: Double = 1.0
       var selectf: Bool = false
       var sepakk: [String: Any]! = [String(cString: [116,105,109,101,0], encoding: .utf8)!:876, String(cString: [98,116,110,0], encoding: .utf8)!:958, String(cString: [99,108,105,112,115,0], encoding: .utf8)!:819]
         selectf = safel > 12.5
         selectf = (sepakk.keys.count + Int(safel)) == 84
          var tabbarh: Double = 2.0
          var logind: [String: Any]! = [String(cString: [108,101,114,112,102,0], encoding: .utf8)!:833, String(cString: [115,101,116,102,100,0], encoding: .utf8)!:219]
          var textN: [String: Any]! = [String(cString: [114,101,110,111,114,109,101,0], encoding: .utf8)!:885, String(cString: [114,101,112,108,97,99,101,0], encoding: .utf8)!:390]
         selectf = tabbarh <= 80.61
         tabbarh *= Double(logind.count)
         logind = ["\(textN.count)": textN.keys.count >> (KCenterTool17.min(labs(2), 3))]
      for _ in 0 ..< 2 {
         sepakk = ["\(selectf)": Int(safel) & 1]
      }
      for _ in 0 ..< 1 {
         safel /= KCenterTool17.max(1, (Double((selectf ? 5 : 3) >> (KCenterTool17.min(labs(1), 1)))))
      }
      while (sepakk.values.count >= 1 || (sepakk.values.count | 1) >= 4) {
          var detailsE: String! = String(cString: [108,117,109,98,101,114,106,97,99,107,0], encoding: .utf8)!
          var parameters_: Double = 2.0
          var headersu: Int = 1
          var resourcesj: Double = 2.0
         selectf = detailsE.count >= 99
         detailsE.append("\(Int(parameters_) << (KCenterTool17.min(5, labs(Int(resourcesj)))))")
         parameters_ /= KCenterTool17.max(4, Double(Int(resourcesj) * 1))
         headersu /= KCenterTool17.max(headersu << (KCenterTool17.min(3, labs(1))), 3)
         break
      }
          var launchM: Bool = true
          var itemz: Bool = false
          var bundleK: Float = 0.0
         safel += Double(Int(safel) + 3)
         launchM = 45.74 <= bundleK
         itemz = 96.32 < bundleK && itemz
       var l_widthf: String! = String(cString: [99,117,114,118,101,0], encoding: .utf8)!
       var collv: String! = String(cString: [109,97,105,110,0], encoding: .utf8)!
      repeat {
         collv = "\(Int(safel) << (KCenterTool17.min(4, labs(3))))"
         if (String(cString:[50,52,107,51,49,50,98,0], encoding: .utf8)!) == collv {
            break
         }
      } while (collv.count > l_widthf.count) && ((String(cString:[50,52,107,51,49,50,98,0], encoding: .utf8)!) == collv)
      navigation9.append("\(((String(cString:[95,0], encoding: .utf8)!) == z_animation0 ? navigation9.count : z_animation0.count))")
        super.viewDidLoad()
   repeat {
      z_animation0 = "\(3 - navigation9.count)"
      if z_animation0 == (String(cString:[109,109,108,51,111,53,48,95,118,109,0], encoding: .utf8)!) {
         break
      }
   } while (!navigation9.contains(z_animation0)) && (z_animation0 == (String(cString:[109,109,108,51,111,53,48,95,118,109,0], encoding: .utf8)!))
        
        tabBar.barTintColor = .white
      navigation9 = "\(((String(cString:[99,0], encoding: .utf8)!) == navigation9 ? navigation9.count : z_animation0.count))"
        tabBar.backgroundColor = UIColor(red: 245/255, green: 248/255, blue: 255/255, alpha: 1.0)
        tabBar.tintColor = .white

        let navigationController = [
            [ZGDSearchController(), "对话"],
            [BFirstController(), "创作"],
            [MLoginPlayController(), "工具"],
            [UGNewsController(), "我的"]
        ]
        
        for item in navigationController {

            let userdataItem: UITabBarItem = UITabBarItem(title: item[1] as? String, image: nil, selectedImage: nil)
                let launchController = KScreenController(rootViewController: item[0] as! UIViewController)
                
                let home: [NSAttributedString.Key: Any] = [
                    .foregroundColor: UIColor(red: 51/255, green: 51/255, blue: 51/255, alpha: 0.5),
                    .font: UIFont.systemFont(ofSize: 16)
                ]
                
                let button: [NSAttributedString.Key: Any] = [
                    .foregroundColor: UIColor(red: 51/255, green: 51/255, blue: 51/255, alpha: 1.0),
                    .font: UIFont.boldSystemFont(ofSize: 16)
                ]
            
                userdataItem.setTitleTextAttributes(home, for: .normal)
                userdataItem.setTitleTextAttributes(button, for: .selected)
                userdataItem.titlePositionAdjustment = UIOffset(horizontal: 0, vertical: -14)
                launchController.tabBarItem = userdataItem
                
                self.addChild(launchController)
        }
    }

    
    override func viewWillAppear(_ animated: Bool) {
       var savebsw: String! = String(cString: [113,117,97,114,116,101,114,0], encoding: .utf8)!
    var clearD: String! = String(cString: [108,117,114,97,108,105,122,97,116,105,111,110,0], encoding: .utf8)!
   if clearD != savebsw {
       var tableO: String! = String(cString: [115,101,109,97,110,116,105,99,0], encoding: .utf8)!
       var requestY: Double = 3.0
       var sublyouta: Bool = true
         tableO.append("\((tableO == (String(cString:[104,0], encoding: .utf8)!) ? tableO.count : (sublyouta ? 3 : 1)))")
          var headersJ: Double = 3.0
         sublyouta = 15.84 < headersJ
      if tableO.contains("\(requestY)") {
         requestY /= KCenterTool17.max(4, Double(tableO.count / 2))
      }
         requestY -= (Double((sublyouta ? 1 : 5)))
         requestY -= Double(tableO.count)
       var modityj: [Any]! = [false]
          var engineL: String! = String(cString: [102,105,110,100,101,112,0], encoding: .utf8)!
          var regions: String! = String(cString: [100,101,99,114,121,112,116,101,100,0], encoding: .utf8)!
          var taskj: Bool = true
         requestY *= Double(engineL.count)
         engineL = "\((1 - (taskj ? 4 : 2)))"
         regions = "\(((String(cString:[83,0], encoding: .utf8)!) == regions ? (taskj ? 5 : 3) : regions.count))"
          var infoX: String! = String(cString: [117,110,98,108,111,99,107,0], encoding: .utf8)!
          var boardy2: Bool = true
          var collection8: String! = String(cString: [114,97,100,98,103,0], encoding: .utf8)!
         requestY /= KCenterTool17.max(2, (Double(1 / (KCenterTool17.max(10, (sublyouta ? 2 : 5))))))
         infoX.append("\(((String(cString:[83,0], encoding: .utf8)!) == collection8 ? collection8.count : infoX.count))")
         boardy2 = collection8 == infoX
          var msgB: String! = String(cString: [118,97,114,105,97,110,99,101,120,0], encoding: .utf8)!
          var inputK: Double = 1.0
          var speedsR: Double = 5.0
         requestY *= (Double((sublyouta ? 5 : 4)))
         msgB.append("\(Int(inputK) * 2)")
         inputK /= KCenterTool17.max(Double(Int(speedsR) - 2), 4)
         speedsR -= Double(Int(speedsR) / (KCenterTool17.max(msgB.count, 6)))
      savebsw = "\(savebsw.count % 3)"
   }
      clearD.append("\(savebsw.count - clearD.count)")

   while (clearD == String(cString:[108,0], encoding: .utf8)! || savebsw != String(cString:[102,0], encoding: .utf8)!) {
       var nextU: String! = String(cString: [99,104,97,116,115,0], encoding: .utf8)!
       var recognitions: Bool = false
       var contentlabelI: [String: Any]! = [String(cString: [108,111,99,97,108,105,122,97,116,105,111,110,115,0], encoding: .utf8)!:7812.0]
       var navigationT: Bool = false
       var baseb: Float = 5.0
      repeat {
         navigationT = (navigationT ? !recognitions : navigationT)
         if navigationT ? !navigationT : navigationT {
            break
         }
      } while (!recognitions) && (navigationT ? !navigationT : navigationT)
      if !recognitions {
         navigationT = !recognitions
      }
          var rowO: Int = 0
         navigationT = Int(baseb) > contentlabelI.keys.count
         rowO %= KCenterTool17.max(3, rowO / (KCenterTool17.max(3, 5)))
      while (nextU.count <= 2 && navigationT) {
          var detailH: String! = String(cString: [97,110,105,109,97,116,105,111,110,0], encoding: .utf8)!
         navigationT = recognitions && detailH.count < 25
         break
      }
      repeat {
         nextU = "\((Int(baseb) ^ (recognitions ? 5 : 5)))"
         if nextU == (String(cString:[53,120,121,95,51,118,0], encoding: .utf8)!) {
            break
         }
      } while (nextU == (String(cString:[53,120,121,95,51,118,0], encoding: .utf8)!)) && (!nextU.hasSuffix("\(contentlabelI.values.count)"))
      for _ in 0 ..< 1 {
          var hasj: String! = String(cString: [99,111,118,101,114,0], encoding: .utf8)!
          var speakV: String! = String(cString: [108,111,115,115,108,101,115,115,0], encoding: .utf8)!
          var alabelm: String! = String(cString: [117,110,105,110,115,116,97,108,108,0], encoding: .utf8)!
         navigationT = 54 <= contentlabelI.keys.count
         hasj = "\(alabelm.count % 1)"
         speakV = "\(hasj.count ^ speakV.count)"
         alabelm.append("\(1)")
      }
       var cell0: Bool = false
       var sectionsG: Bool = false
      while (!sectionsG) {
         cell0 = !navigationT
         break
      }
         recognitions = !sectionsG
       var sendy: [Any]! = [980, 200, 844]
      if !sendy.contains { $0 as? Float == baseb } {
          var enabledf: [String: Any]! = [String(cString: [110,116,105,108,101,0], encoding: .utf8)!:968, String(cString: [114,101,101,110,99,114,121,112,116,0], encoding: .utf8)!:194]
          var a_player2: String! = String(cString: [117,110,98,111,110,100,0], encoding: .utf8)!
         sendy.append(2)
         enabledf = ["\(enabledf.count)": a_player2.count]
         a_player2 = "\((a_player2 == (String(cString:[99,0], encoding: .utf8)!) ? enabledf.keys.count : a_player2.count))"
      }
      repeat {
          var m_heighta: String! = String(cString: [100,121,110,108,111,99,107,0], encoding: .utf8)!
          var decibel1: Double = 4.0
         nextU.append("\(contentlabelI.count % (KCenterTool17.max(3, 1)))")
         m_heighta = "\((m_heighta == (String(cString:[79,0], encoding: .utf8)!) ? m_heighta.count : Int(decibel1)))"
         decibel1 *= Double(Int(decibel1) * 1)
         if 510663 == nextU.count {
            break
         }
      } while (510663 == nextU.count) && (!nextU.contains("\(baseb)"))
      while (4 > (nextU.count / (KCenterTool17.max(3, 6))) && 4 > (3 / (KCenterTool17.max(4, contentlabelI.keys.count)))) {
         contentlabelI = ["\(contentlabelI.values.count)": 2]
         break
      }
         navigationT = (55 >= (sendy.count * (navigationT ? 55 : sendy.count)))
      if !recognitions || !navigationT {
         navigationT = !sectionsG
      }
      savebsw.append("\((contentlabelI.count >> (KCenterTool17.min(5, labs((navigationT ? 2 : 2))))))")
      break
   }
        super.viewWillAppear(animated)
    }

}

 struct KCenterTool17 {
    static func min<F: Comparable>(_ t1: F, _ t2: F) -> F {
        return t1 > t2 ? t2 : t1
    }
    
    static func max<F: Comparable>(_ t1: F, _ t2: F) -> F {
        return t1 > t2 ? t1 : t2
    }
}